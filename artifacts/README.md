# Artifacts Directory

This folder stores saved model artifacts and metadata.

- `models/`: trained model files used after pipeline runs
- `checkpoints/`: checkpoint files for sequence models
- `metadata/`: JSON metadata about the selected final model
